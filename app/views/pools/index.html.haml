%section
	%form{:action => '/pools/save', :method => 'post'}
		%ul
			- @proposals.each do |f|
				%li
					=f.title
					=check_box_tag 'proposals[]', f.id
					
		%input{:type => 'submit', :value => 'save'}